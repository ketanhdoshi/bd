# Spring properties
spring:
  # Set the application name, so the service registers under this 
  # name and can also be accessed by this name 
  application:
     name: web-service
  # Oauth Github settings
  security:  
    oauth2:  
      client:  
        registration:  
          github:  
            clientId: 99bc2dfb796f77af706e  
            clientSecret: c7d072014a22de5249761cd36a6a2c9feec232e6

# Discovery Server Access
eureka:
  client:
    # URL of the Eureka Discovery Server
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

# Web Server listens on the default HTTP (Tomcat) port 8080

# Secret key for signing JWT tokens
jwt:
  secret: challokd
  jwtExpirationInMs: 3600000  # 1 hour in milliseconds


